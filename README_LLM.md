# Travelhoop - Модульный монолит на Node.js

Travelhoop — это демонстрационный проект, реализованный на Node.js с использованием архитектурного паттерна «Модульный монолит». Основная идея проекта — высокая степень изоляции модулей друг от друга, что позволяет разрабатывать их независимо и, при необходимости, легко выносить в отдельные микросервисы.

## Что такое модульный монолит?

**Модульный монолит** — это архитектурный паттерн, при котором приложение проектируется как единый процесс (один деплой, одна база данных), но внутри разделено на независимые, слабо связанные модули. Каждый модуль отвечает за свою бизнес-область и имеет четкие границы.

### Отличие от простого монолита
В **простом монолите** (Big Ball of Mud) код организован по техническим слоям: все контроллеры в одной папке, все модели в другой. Это ведет к запутанным связям.
В **модульном монолите** главное — **инкапсуляция**. Каждый модуль (`booking`, `couch`, `user`, `review`) содержит внутри себя всё необходимое: свои роуты, бизнес-логику (Domain), сущности базы данных и репозитории. Модули не обращаются к коду друг друга напрямую.

---

## Как устроен проект

### 1. Структура модулей
В папке `src/modules` каждый модуль — это практически маленькое независимое приложение. Типичная структура:
*   `src/api`: внешние эндпоинты (роуты).
*   `src/domain`: бизнес-логика и правила (Entities, Aggregates).
*   `src/infrastructure`: реализация репозиториев, схемы БД и настройки.

### 2. Изолированные API
У каждого модуля **свой API**. Например, для модуля `booking` базовый путь — `/booking`. Все его ручки (например, `/request-booking`) доступны только через этот префикс. Это гарантирует, что границы ответственности модулей четко определены на уровне интерфейса.

### 3. Сборка и запуск (App)
Основное приложение находится в `src/app/travelhoop`. Оно работает как «хост»:
*   Динамически загружает модули из папки `@travelhoop`.
*   Регистрирует роуты всех модулей в один общий Express-инстанс.
*   Пробрасывает общие зависимости (соединение с БД, Redis) во все модули.

---

## Взаимодействие между модулями

### Зачем модулям API, если есть события?
**API нужен для людей и внешних систем, а события — для синхронизации данных внутри системы.**
*   **API (Входная точка):** Команды "Создай коуч" или "Забронируй" приходят через API. Событие не может возникнуть из ниоткуда.
*   **События (Нервная система):** Когда в модуле `couch` создается жилье, он выбрасывает событие `CouchCreated`. Модуль `booking` ловит его и создает у себя локальную копию данных, чтобы знать, что это жилье теперь можно бронировать.

### Как это работает "под капотом"

В проекте используется два типа доставки событий:

1.  **In-Memory (Внутри модуля):**
    *   **Когда:** Используется для связи компонентов внутри одного и того же модуля.
    *   **Механизм:** `InMemoryEventDispatcher` сразу вызывает функции подписчиков в том же процессе.
    *   **Пример:** В модуле `booking` при создании бронирования может сработать внутренний логгер.

2.  **Redis / Message Broker (Между модулями):**
    *   **Когда:** Используется, когда один модуль должен сообщить другому о важном изменении.
    *   **Механизм:** 
        1. Модуль вызывает `messageBroker.publish(event)`.
        2. Событие превращается в JSON и улетает в **Redis**.
        3. **Background Worker** (отдельный процесс) достает его и рассылает по всем остальным модулям.
    *   **Пример:** Модуль `couch` создал жилье → отправил в Redis → модуль `booking` узнал об этом и разрешил бронирование.

---

## Пример сквозного процесса (Request Booking)

Разберем, как все 4 типа стикеров Event Storming работают вместе на примере процесса запроса бронирования:

1.  🟦 **COMMAND: Request booking (Запросить бронирование)**
    *   Пользователь нажимает кнопку "Забронировать" на фронтенде.
    *   Отправляется HTTP POST запрос на `/booking/request-booking`.
    *   В коде это превращается в `RequestCouchBookingCommand`.

2.  🟪 **POLICY: Booking availability policy (Правила бронирования)**
    *   Прежде чем создать запрос, система должна проверить правила.
    *   В коде `BookableCouch.canBook()` проверяет: 
        - Не является ли гость хозяином этого жилья?
        - Активно ли жилье (не в архиве)?
        - Нет ли на эти даты других подтвержденных бронирований?

3.  🟨 **AGGREGATE: CouchBookingRequest (Запрос на бронирование)**
    *   Если все правила (Policy) пройдены, система создает новый объект (Агрегат) `CouchBookingRequest`.
    *   Этот объект сохраняется в базу данных в таблицу `couch_booking_request`.

4.  🟧 **EVENT: CouchBookingRequestCreated (Запрос создан)**
    *   Как только агрегат создан, он генерирует событие.
    *   Это событие сообщает системе: "Запрос №123 успешно создан, можно двигаться дальше (например, уведомить хозяина)".

---

## Event Storming - полное объяснение

**Event Storming** — это метод моделирования бизнес-процессов. В нашем проекте на диаграммах показано, как работают модули User и Host в Travelhoop.

### Четыре типа стикеров:

*   🟦 **Синий стикер — COMMAND (команда)**
    Это приказ/действие, которое кто-то (пользователь или система) выполняет.
    *Примеры с картинки:*
    - `Create user` — создать пользователя
    - `Update user` — обновить пользователя
    - `Add couch` — добавить жилье
    - `Update couch` — обновить жилье
    - `Archive couch` — заархивировать жилье
    Команды — это входящие данные. Пользователь кликает кнопку "Зарегистрироваться" → выполняется команда `Create user`.

*   🟨 **Жёлтый стикер — AGGREGATE (агрегат)**
    Это объект/сущность, которая хранит данные и бизнес-логику. Агрегат — это что-то, что изменяется при выполнении команды.
    *Примеры с картинки:*
    - `User` — объект пользователя (имя, email, пароль, профиль)
    - `Couch` — объект жилья (адрес, описание, доступность)

*   🟧 **Оранжевый стикер — EVENT (событие)**
    Это то, что произошло в системе. События всегда в прошедшем времени и являются результатом выполнения команды.
    *Примеры с картинки:*
    - `User registered` — пользователь зарегистрировался
    - `User updated` — пользователь обновил профиль
    - `Couch added` — жилье добавлено
    - `Couch updated` — жилье обновлено
    - `Couch archived` — жилье заархивировано

*   🟪 **Фиолетовый стикер — POLICY (политика/правило)**
    Это бизнес-правило, условие, которое ограничивает выполнение команды.
    *Пример с картинки:*
    - `Couch archivisation policy` — правило архивирования жилья (например: "хозяин может заархивировать жилье только если у него нет активных бронирований").

---

## Сравнение с другими архитектурами
Детальное сравнение модульного монолита с SOA и микросервисами вы найдете в файле [README_ARCHITECTURE.md](./README_ARCHITECTURE.md).

---

## Как запустить проект

### Локальная разработка
1. Установите Rush глобально: `npm install @microsoft/rush -g`
2. Установите зависимости: `rush update`
3. Соберите проект: `rush build`
4. Скопируйте конфиг: `cp src/app/travelhoop/.env.dist src/app/travelhoop/.env`
5. Запустите инфраструктуру: `docker-compose up -d`
6. Запустите проект (в разных терминалах):
   - `rush build:watch`
   - `cd src/app/travelhoop && npm run start-dev`
